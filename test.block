>> Define variables
x = 1
y = -2
strings = "hello"
multi = "line1
line2
line3"

// Conditionals
if x > 0:
    echo("X is positive")
elif x < 0:
    echo("X is negative")
else:
    echo("X is zero")

# while loop
while x < 11:
    echo(x)
    x = x + 1

# for loop
for i in 1..2:
    echo(i)

#> functions
fn add[x, y=2]:
    -> x + y

z = add(1)
lst = [0, 1, 2, 3]
first = lst[0]
slc = lst[0,2]
echo(first, slc)
dct = [0: 1, 1: 2, 2:3]
echo(typeOf(dct))
echo(slc)
echo(first)
echo(z)
echo(typeOf(add))
x = "10"
echo(typeOf(toNum(x)))
echo(true)